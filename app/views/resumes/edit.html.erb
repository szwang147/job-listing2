<h1> 修改简历，投递简历到<%= @job.title %></h1>
<hr>
<%= simple_form_for [@job, @resume] do |f| %>
  <%= f.input :content %>
  <%= f.input :attachment %>
  <%= f.submit "修改完毕，发送简历", class: "btn btn-primary" %>
  <% end %>
